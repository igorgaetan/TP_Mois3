# IMPORTANT : ce fichier est un TEMPLATE. Les valeurs réelles ne sont JAMAIS committées.
# En pratique, le secret est créé par le pipeline Jenkins (depuis AWS Secrets Manager)
# ou via Ansible Vault pour l'EC2 k8s. Exemple de commande utilisée par Jenkins :
#
# kubectl create secret generic db-credentials -n capstone \
#   --from-literal=DB_HOST=$DB_HOST \
#   --from-literal=DB_PORT=5432 \
#   --from-literal=DB_NAME=$DB_NAME \
#   --from-literal=DB_USER=$DB_USER \
#   --from-literal=DB_PASSWORD=$DB_PASSWORD \
#   --dry-run=client -o yaml | kubectl apply -f -
#
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: capstone
type: Opaque
stringData:
  DB_HOST: "__REPLACED_AT_DEPLOY_TIME__"
  DB_PORT: "5432"
  DB_NAME: "__REPLACED_AT_DEPLOY_TIME__"
  DB_USER: "__REPLACED_AT_DEPLOY_TIME__"
  DB_PASSWORD: "__REPLACED_AT_DEPLOY_TIME__"
